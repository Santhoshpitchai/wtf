# ============================================
# Supabase Configuration
# ============================================
# Your Supabase project URL (found in project settings)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# Supabase anonymous key (found in project settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase service role key (found in project settings > API)
# WARNING: Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ============================================
# Application Configuration
# ============================================
# Base URL of your application
# REQUIRED for session verification emails to generate correct links
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Email Service Configuration (Resend)
# ============================================
# Resend API key for sending verification emails
# REQUIRED for client session verification feature
# Get your API key from: https://resend.com/api-keys
# 
# Without this key:
# - Verification emails will NOT be sent
# - System will run in development mode
# - Verification URLs will be logged to console only
# 
# To set up:
# 1. Sign up at https://resend.com
# 2. Create an API key
# 3. Add the key below
# 4. Verify your sending domain (for production)
RESEND_API_KEY=your_resend_api_key_here

# Email sender address for verification emails
# OPTIONAL - defaults to 'WTF Fitness <onboarding@resend.dev>'
# For production, use your verified domain:
# Example: WTF Fitness <noreply@yourdomain.com>
# 
# To verify your domain in Resend:
# 1. Go to https://resend.com/domains
# 2. Add your domain
# 3. Add the DNS records provided by Resend
# 4. Wait for verification (usually a few minutes)
# 5. Update this variable with your verified domain email
RESEND_FROM_EMAIL=WTF Fitness <onboarding@resend.dev>

# ============================================
# Cleanup API Configuration
# ============================================
# Secure random key for automated cleanup endpoint
# Generate a secure random key for production use
# Example: openssl rand -hex 32
CLEANUP_API_KEY=your_secure_random_key_here
